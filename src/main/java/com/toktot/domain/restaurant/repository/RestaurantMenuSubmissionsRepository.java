package com.toktot.domain.restaurant.repository;

import com.toktot.domain.restaurant.RestaurantMenuSubmissions;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RestaurantMenuSubmissionsRepository extends JpaRepository<RestaurantMenuSubmissions, Long> {
}
